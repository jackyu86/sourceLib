package io.sited.http.client.local.module;

import io.sited.Module;
import io.sited.ModuleInfo;

/**
 * @author chi
 */
@ModuleInfo(name = "local")
public class LocalModule extends Module {
    @Override
    protected void configure() throws Exception {
    }
}
