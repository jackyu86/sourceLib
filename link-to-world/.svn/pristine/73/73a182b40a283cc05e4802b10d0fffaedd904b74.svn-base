package io.sited.template.impl;

import io.sited.template.AttributeWriter;
import io.sited.template.Element;
import io.sited.template.ElementWriter;
import io.sited.template.Node;

/**
 * @author chi
 */
public interface ElementCompiler {
    Element compile(Node node);

    void setElementWriter(String elementName, ElementWriter elementWriter);

    void setAttributeWriter(String attributeName, AttributeWriter attributeWriter);
}
