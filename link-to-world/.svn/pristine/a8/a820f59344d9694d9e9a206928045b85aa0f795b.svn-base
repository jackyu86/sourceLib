package io.sited.i18n;

import java.util.Locale;
import java.util.Optional;

/**
 * @author chi
 */
public interface MessageRepository extends Iterable<Message> {
    Locale locale();

    Optional<Message> message(String key);
}
