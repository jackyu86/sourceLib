package io.sited.customer.web;

/**
 * @author chi
 */
public class CountryWebServiceImplTest {
    /*@Rule
    public final SiteRule site = new SiteRule(new CustomerModuleImpl());

    @Before
    public void setup() {
        CountryService countryService = site.module(CustomerModule.class).require(CountryService.class);
        CreateCountryRequest country = new CreateCountryRequest();
        country.countryCode = "cn";
        country.flagImageURL = "cn.flag";
        country.displayName = "中国";
        country.requestBy = "init";
        country.currencies = Lists.newArrayList("CNY");
        countryService.create(country);
    }

    @Test
    public void all() throws Exception {
        ServerResponse response = site.get("/api/country").execute();
        List<CountryResponse> countryResponses = response.body(List.class);
        Assert.assertEquals(1, countryResponses.size());
    }

    @Test
    public void get() throws Exception {
        Optional response = site.get("/api/country/code/cn").execute().body(Optional.class);
        Assert.assertNotNull(response.isPresent());
    }*/
}